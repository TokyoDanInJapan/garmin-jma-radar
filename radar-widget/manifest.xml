<?xml version="1.0"?>
<!--
  Connect IQ manifest for the JMA Rain Radar widget.

  Permissions:
    Communications - HTTP image + JSON requests via the phone (Edge has no cell)
    Positioning    - GPS to center radar on the rider

  Products: a few representative Edge devices. Add/remove to match your targets.
  Verify the exact product IDs in the Connect IQ SDK device list.
-->
<iq:manifest xmlns:iq="http://www.garmin.com/xml/connectiq" version="3">
  <iq:application
      id="61c8f976823a440aa1bedfa3c073f740"
      type="widget"
      name="@Strings.AppName"
      entry="RainRadarApp"
      launcherIcon="@Drawables.LauncherIcon"
      minSdkVersion="3.2.0">

    <iq:products>
      <iq:product id="edge1030plus"/>
      <iq:product id="edge1040"/>
    </iq:products>

    <iq:permissions>
      <iq:uses-permission id="Communications"/>
      <iq:uses-permission id="Positioning"/>
    </iq:permissions>

    <iq:languages>
      <iq:language>eng</iq:language>
    </iq:languages>
  </iq:application>
</iq:manifest>
